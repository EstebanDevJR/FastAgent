# innovation-agent

Generated by FastAgent.

## Project Architect Summary

- Architect provider: local
- Architect model: heuristic
- Agent type: rag
- LLM provider: OpenAI
- Router mode: balanced
- Vector DB: Qdrant
- Memory: hybrid
- Tracing: LangSmith
- Evaluation: enabled
- Suggested models: Llama 3, Mistral, Phi
- Tools: document_search, context_formatter
- Architect notes: RAG pipeline enabled with retriever abstraction. | High-risk domain detected: evaluation forced on by default. | Architect provider 'openai' failed after 3 attempt(s) (OpenAI auto mode failed. responses: OPENAI_API_KEY is not configured; chat: OPENAI_API_KEY is not configured), using local fallback.

## Quickstart

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\\Scripts\\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Commands

- `POST /chat`
- `GET /health`
- `GET /info`
- `POST /evaluate`

## Plugins

```bash
fastagent plugins --project-path .
fastagent add-plugin my_plugin --project-path . --source local
fastagent enable-plugin my_plugin --project-path .
fastagent doctor --project-path .
fastagent bench --base-url http://127.0.0.1:8000 --endpoint /chat
```

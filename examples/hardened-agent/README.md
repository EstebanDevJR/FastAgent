# hardened-agent

Generated by FastAgent.

## Project Architect Summary

- Architect provider: local
- Architect model: heuristic
- Agent type: rag
- LLM provider: OpenAI
- Vector DB: Qdrant
- Memory: conversation
- Tracing: LangSmith
- Evaluation: enabled
- Suggested models: Llama 3, Mistral, Phi
- Tools: contract_parser, clause_risk_analyzer, legal_search
- Architect notes: RAG pipeline enabled with retriever abstraction. | Legal workflow detected: specialized contract tools suggested. | Architect provider 'openai' failed after 2 attempt(s) (OPENAI_API_KEY is not configured), using local fallback.

## Quickstart

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\\Scripts\\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Commands

- `POST /chat`
- `GET /health`
- `POST /evaluate`